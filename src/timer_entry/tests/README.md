# timer_entry tests

`src/timer_entry/tests/` には core のテストを置きます。

想定する内容:

- `backtest_1m.py` と `backtest_fast.py` の parity test
- event time 列の spot check
- `minute_data.py` / `features.py` / `filters.py` の基礎テスト
